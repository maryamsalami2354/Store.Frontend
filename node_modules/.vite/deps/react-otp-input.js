import { r as __toESM } from "./chunk-CYJPkc-J.js";
import { t as require_react } from "./react.js";
//#region node_modules/react-otp-input/lib/index.esm.js
var import_react = /* @__PURE__ */ __toESM(require_react());
var isStyleObject = function(obj) {
	return typeof obj === "object" && obj !== null;
};
var OTPInput = function(_a) {
	var _b = _a.value, value = _b === void 0 ? "" : _b, _c = _a.numInputs, numInputs = _c === void 0 ? 4 : _c, onChange = _a.onChange, onPaste = _a.onPaste, renderInput = _a.renderInput, _d = _a.shouldAutoFocus, shouldAutoFocus = _d === void 0 ? false : _d, _e = _a.inputType, inputType = _e === void 0 ? "text" : _e, renderSeparator = _a.renderSeparator, placeholder = _a.placeholder, containerStyle = _a.containerStyle, inputStyle = _a.inputStyle, _f = _a.skipDefaultStyles, skipDefaultStyles = _f === void 0 ? false : _f;
	var _g = import_react.useState(0), activeInput = _g[0], setActiveInput = _g[1];
	var inputRefs = import_react.useRef([]);
	var getOTPValue = function() {
		return value ? value.toString().split("") : [];
	};
	var isInputNum = inputType === "number" || inputType === "tel";
	import_react.useEffect(function() {
		inputRefs.current = inputRefs.current.slice(0, numInputs);
	}, [numInputs]);
	import_react.useEffect(function() {
		var _a;
		if (shouldAutoFocus) (_a = inputRefs.current[0]) === null || _a === void 0 || _a.focus();
	}, [shouldAutoFocus]);
	var getPlaceholderValue = function() {
		if (typeof placeholder === "string") {
			if (placeholder.length === numInputs) return placeholder;
			if (placeholder.length > 0) console.error("Length of the placeholder should be equal to the number of inputs.");
		}
	};
	var isInputValueValid = function(value) {
		return (isInputNum ? !isNaN(Number(value)) : typeof value === "string") && value.trim().length === 1;
	};
	var handleChange = function(event) {
		var value = event.target.value;
		if (isInputValueValid(value)) {
			changeCodeAtFocus(value);
			focusInput(activeInput + 1);
		}
	};
	var handleInputChange = function(event) {
		var nativeEvent = event.nativeEvent;
		var value = event.target.value;
		if (!isInputValueValid(value)) {
			if (value.length === numInputs) {
				if (!value.split("").some(function(cellInput) {
					return !isInputValueValid(cellInput);
				})) {
					handleOTPChange(value.split(""));
					focusInput(numInputs - 1);
				}
			}
			if (nativeEvent.data === null && nativeEvent.inputType === "deleteContentBackward") {
				event.preventDefault();
				changeCodeAtFocus("");
				focusInput(activeInput - 1);
			}
			event.target.value = "";
		}
	};
	var handleFocus = function(event) {
		return function(index) {
			setActiveInput(index);
			event.target.select();
		};
	};
	var handleBlur = function() {
		setActiveInput(activeInput - 1);
	};
	var handleKeyDown = function(event) {
		var otp = getOTPValue();
		if ([event.code, event.key].includes("Backspace")) {
			event.preventDefault();
			changeCodeAtFocus("");
			focusInput(activeInput - 1);
		} else if (event.code === "Delete") {
			event.preventDefault();
			changeCodeAtFocus("");
		} else if (event.code === "ArrowLeft") {
			event.preventDefault();
			focusInput(activeInput - 1);
		} else if (event.code === "ArrowRight") {
			event.preventDefault();
			focusInput(activeInput + 1);
		} else if (event.key === otp[activeInput]) {
			event.preventDefault();
			focusInput(activeInput + 1);
		} else if (event.code === "Spacebar" || event.code === "Space" || event.code === "ArrowUp" || event.code === "ArrowDown") event.preventDefault();
	};
	var focusInput = function(index) {
		var _a, _b;
		var activeInput = Math.max(Math.min(numInputs - 1, index), 0);
		if (inputRefs.current[activeInput]) {
			(_a = inputRefs.current[activeInput]) === null || _a === void 0 || _a.focus();
			(_b = inputRefs.current[activeInput]) === null || _b === void 0 || _b.select();
			setActiveInput(activeInput);
		}
	};
	var changeCodeAtFocus = function(value) {
		var otp = getOTPValue();
		otp[activeInput] = value[0];
		handleOTPChange(otp);
	};
	var handleOTPChange = function(otp) {
		onChange(otp.join(""));
	};
	var handlePaste = function(event) {
		var _a;
		event.preventDefault();
		var otp = getOTPValue();
		var nextActiveInput = activeInput;
		var pastedData = event.clipboardData.getData("text/plain").slice(0, numInputs - activeInput).split("");
		if (isInputNum && pastedData.some(function(value) {
			return isNaN(Number(value));
		})) return;
		for (var pos = 0; pos < numInputs; ++pos) if (pos >= activeInput && pastedData.length > 0) {
			otp[pos] = (_a = pastedData.shift()) !== null && _a !== void 0 ? _a : "";
			nextActiveInput++;
		}
		focusInput(nextActiveInput);
		handleOTPChange(otp);
	};
	return import_react.createElement("div", {
		style: Object.assign({
			display: "flex",
			alignItems: "center"
		}, isStyleObject(containerStyle) && containerStyle),
		className: typeof containerStyle === "string" ? containerStyle : void 0,
		onPaste
	}, Array.from({ length: numInputs }, function(_, index) {
		return index;
	}).map(function(index) {
		var _a, _b, _c;
		return import_react.createElement(import_react.Fragment, { key: index }, renderInput({
			value: (_a = getOTPValue()[index]) !== null && _a !== void 0 ? _a : "",
			placeholder: (_c = (_b = getPlaceholderValue()) === null || _b === void 0 ? void 0 : _b[index]) !== null && _c !== void 0 ? _c : void 0,
			ref: function(element) {
				return inputRefs.current[index] = element;
			},
			onChange: handleChange,
			onFocus: function(event) {
				return handleFocus(event)(index);
			},
			onBlur: handleBlur,
			onKeyDown: handleKeyDown,
			onPaste: handlePaste,
			autoComplete: "off",
			"aria-label": "Please enter OTP character ".concat(index + 1),
			style: Object.assign(!skipDefaultStyles ? {
				width: "1em",
				textAlign: "center"
			} : {}, isStyleObject(inputStyle) ? inputStyle : {}),
			className: typeof inputStyle === "string" ? inputStyle : void 0,
			type: inputType,
			inputMode: isInputNum ? "numeric" : "text",
			onInput: handleInputChange
		}, index), index < numInputs - 1 && (typeof renderSeparator === "function" ? renderSeparator(index) : renderSeparator));
	}));
};
//#endregion
export { OTPInput as default };

//# sourceMappingURL=react-otp-input.js.map